# 09 — Let the architecture close as one

Type: verification and cleanup

Status: resolved (2026-07-29)

Blocked by: ticket 08.

## Outcome

The Rust-first convergence closes with one green non-live baseline, durable
architecture ratchets, and planning records that agree with implemented Git
history.

## Scope

1. Add or tighten architecture tests for:
   - the unchanged Rust interface and source manifest;
   - typed Python authority views;
   - exhaustive compiler/application method binding;
   - propagation-only 8/12/16 branch semantics;
   - retained partial-quantization refusal reports without fake branches;
   - geometric single-cell continuous orientations;
   - exhaustive Lumerical product dialect;
   - one qualification path;
   - private Lumerical work life;
   - private shared field storage;
   - dependency direction and forbidden retired names.
2. Remove shallow tests whose behavior is now covered through deepened
   interfaces. Replace; do not layer.
3. Remove only imports, helpers, aliases, and files made obsolete by tickets
   01–08.
4. Reconcile `.scratch/metalens-sonnet-convergence` and later repair tickets
   with the actual committed baseline. Preserve historical evidence; mark
   superseded planning clearly rather than rewriting it.
5. Record one complete non-live verification result.

## Acceptance

- All focused ticket tests remain green.
- The complete non-live suite passes once with live markers deselected.
- Pyright reports zero errors.
- Every touched production file has zero CSU hard violations.
- `git diff --check` is empty.
- The Rust source manifest verifies and the full Rust gate remains green.
- No runtime import cycle is introduced.
- No old public name, compatibility alias, raw authority view parsing,
  guessed native property, public sweep implementation, or duplicate array
  restoration remains.
- Active specs and ticket statuses agree with Git implementation.

## Test discipline

Use
`C:\Users\Administrator\miniforge3\envs\research_env\python.exe`
for every Python command. Run the complete non-live suite only after the
focused and architecture slices are green.

## Out of scope

- the four canonical live briefs;
- a real adviser call or native solver run;
- changing brief physics to make a test pass;
- large-na, optimization, achromatic, holographic, quasi-BIC, or
  frequency-selective implementation;
- repo-wide naming or CSU cleanup unrelated to tickets 01–08.

## Do not add

Do not add further implementation tickets merely to absorb a local defect.
If verification exposes a distinct product or scientific problem, retain its
diagnostics and report it for a later effort.
