# Build the exact bounded CellStudyPlan

Status: resolved (2026-08-10)
Labels: `ready-for-agent`
Depends on: 02

Introduce the deep pure `cell_study` Module. It derives the fixed Method
aperture, legal heights and lateral candidates, route forecasts, deterministic
bounded coverage, fabrication cautions, response obligations, and exact work
count. The immutable plan owns every work item and exposes stable identity and
serialization. Propagation and PB are a closed typed union. The plan must
refuse invalid extent/fabrication inputs without touching solver, filesystem,
Authority, or harness transports.
