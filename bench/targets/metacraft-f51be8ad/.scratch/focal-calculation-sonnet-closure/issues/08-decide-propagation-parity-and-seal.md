# Assess propagation parity and seal

Status: ready-for-agent

Resolution: open

Parity evidence: implemented as `dual_applicability`; the seal remains
`incomplete` until Ticket 07 real Codex/Claude acceptance and both named Native
delivery endpoints pass. No final manifest has been generated.
The strict public matrix now covers the circular/linear PB projection, the
470/480 nm sampling boundary, axial focus, x/y FWHM, depth, normalized
transmission/concentration/focus efficiency, explicit non-comparable absolute
power, CPU/CUDA runtime and memory, and the independently recomputable
four-role compact Result signatures.

Native evidence: the one authorized two-endpoint execution on 2026-08-13
finished `2 failed` in 5.7721233 seconds. Both fresh application roots had a
missing shared parent, so command failed before Lumerical execution. The gate
was not retried; both endpoints remain blockers.

Corrected Native evidence: after the parent directory was supplied, the one
authorized rerun finished `2 failed` in 7.7912642 seconds. Both endpoints
reached Adapter observation but failed
`lumerical_unavailable:configuration_incomplete` before solver execution. No
further retry is authorized; both endpoints remain blockers.

Blocked by: 07

Parent: [map](../map.md) · [specification](../spec.md)

## Outcome

Evidence—not aesthetic preference—decides whether electromagnetic propagation
is ready to replace component propagation. The initiative then seals the
current truthful architecture without hiding a migration inside its terminal
ticket.

## Implementation

Build a bounded parity matrix through the common `Field -> FocalRegion -> Focus`
Interface. Cover low-NA transverse-linear propagation, both PB incident
handedness cases with explicit circular/linear projection, period/sampling edge
cases, complex field shape, axial focus, FWHM, depth, transmission,
concentration, absolute power meaning, CPU/CUDA, memory, and the low-NA
propagation/PB recorded Results. Record runtime and peak-memory evidence but do
not let speed excuse scientific drift.

The decision has two allowed records:

1. if every frozen claim passes and the PB measurement-channel model remains
   natural, record replacement readiness and create a bounded successor
   spec/ticket proposal for separate maintainer approval; do not modify current
   propagation routes or bindings in this ticket;
2. otherwise retain both realizations, document their exact applicability and
   failed parity claims, and close without further refactoring.

After the decision, run the complete deterministic suite, architecture suite,
Pyright, CSU, CPU, available native CUDA, four recorded journeys, two available
Native endpoints, installed-root/package/public-surface/Result replay gates,
and fresh Standards + Spec/science review. Update active architecture docs and
ADR 0026 only to present truth, then produce a reproducible candidate manifest
that excludes clone-local `.scratch` planning records and close Tickets 01-08
atomically.

## Acceptance

- The parity report contains exact fixtures, formulas, tolerances, devices,
  timings, memory, results and failed claims; no conclusion is inferred from NA
  alone.
- The final tree retains the current two explicitly applicable propagation
  realizations and no hidden runtime preference. A positive replacement verdict
  exists only as a proposed successor, never as partially migrated code.
- `conduct` remains the only Harness Interface; FFT/CZT joint qualification and
  terminal replay remain intact.
- No High/Important review finding, skipped required gate, stale active doc,
  unresolved ticket, broken link, untracked required evidence, or dirty sealed
  manifest remains.

## Guardrails

This ticket is the stop. It must not migrate or add a propagation method,
optimize unrelated periodic/cell-study code, or continue refactoring because
the tree could be made aesthetically different. A successor proposal is
planning evidence only and cannot be implemented without new approval.
