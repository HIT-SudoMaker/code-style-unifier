# 05 — Let the architecture close without residue

Type: implementation

Status: resolved (2026-07-29)

Blocked by: tickets 02, 03, and 04.

## Outcome

The repaired architecture ends with one owner for each shared mechanism, exact
dependency ratchets, no dead scientific path, one green non-live baseline,
and planning records that agree with the implemented Git history.

## Scope

1. Implement the common metalens proof tail once while preserving distinct
   propagation and geometric response evidence.
2. Implement binary component mapping and storage descriptors once inside the
   private field implementation used by Field and FocalRegion.
3. Keep their public document schemas and scientific meanings distinct.
4. Tighten the field architecture ratchet so it recognizes exactly the
   allowed level-three private importers and fails for every new importer.
5. Validate common capabilities and product bindings whenever a branch family
   is restored.
6. Delete the orphaned geometric observation decoder and every helper or test
   made unreachable by its removal.
7. Remove shallow or obsolete architecture assertions only when a stronger
   public-seam or exact dependency test replaces them.
8. Reconcile the old authority-and-science closure as implemented and
   continued by this road.
9. Mark the historical live ticket as superseded by this road's human gate.
10. Leave a durable closure report beside this specification rather than only
    inside an ignored agent ledger.
11. Run the complete non-live baseline once after all preceding changes have
    converged.

## Acceptance

- The common metalens proof tail has one implementation and one owner.
- Field and FocalRegion share one private component-storage mechanism without
  sharing or conflating their public schemas.
- The architecture ratchet admits only the intended private field importers
  and catches one synthetic forbidden importer.
- Restored siblings with mismatched common capabilities or bindings fail
  before scientific advancement.
- The dead geometric decoder and its orphaned helpers are absent.
- No compatibility alias, registry, framework, or speculative abstraction was
  introduced during cleanup.
- The previous closure specification is marked implemented and points to this
  continuation.
- The previous live ticket is marked superseded and points to ticket 06.
- This map, specification, tickets, Git commits, and test report tell the same
  lifecycle story.
- The complete non-live suite has zero failures and zero unexpected skips.
- Pyright reports zero errors.
- CSU reports zero hard violations in touched production files.
- Rust remains unchanged after ticket 01 and its frozen source manifest
  verifies.

## Focused tests

- public Field and FocalRegion round trips through their distinct document
  schemas;
- exact private-field importer allowlist and a forbidden-import regression;
- common proof-tail equivalence for propagation and geometric results;
- restored frontier context mismatch;
- architecture scans for the retired decoder, duplicate storage mechanics,
  duplicate proof tail, old capability names, and string-classified expected
  failures;
- tracked Markdown link and issue-lifecycle validation.

## Verification

Use the repository Python interpreter for the complete non-live suite. Also
run:

- Pyright;
- CSU on every touched production file;
- all relevant architecture tests;
- Rust source-manifest verification;
- an audit showing no Rust production change after ticket 01;
- tracked Markdown link validation;
- `git diff --check`;
- a clean-status review that preserves unrelated user files.

Record exact commands, test counts, deselections, timings, and commit
identities in the durable closure report. Do not infer success from a prior
agent ledger.

## Stop and report

Stop if cleanup exposes a scientific behavior change, requires a public schema
merge, or needs a broad package rewrite. Preserve a focused failing test and
open a separate bounded ticket rather than hiding the defect inside closure.

## Do not add

Do not add a common science base class, generic document registry, storage
framework, public field-internals Interface, compatibility alias, new result
schema, broad naming campaign, or live solver test.

## Resolution

Commit `aa30da2` removed the remaining field-storage reach, orphaned geometric
decoder, and stale test hashes. Commit `ca90c27` completed repository hygiene;
the integrated non-live baseline closed with 649 passing tests.
