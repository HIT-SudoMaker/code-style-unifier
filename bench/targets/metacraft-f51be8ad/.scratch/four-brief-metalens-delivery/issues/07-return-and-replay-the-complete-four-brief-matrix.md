# 07 — Return and replay the complete four-brief matrix

**Type:** implementation and delivery verification

**Status:** wontfix

**Superseded by:** `../../metalens-sonnet-convergence/spec.md`

**Blocked by:** tickets 05 and 06.

## Outcome

The four canonical briefs complete with real Lumerical evidence and return
exactly eight admitted, replayable Results.

## What to build

- Run the two complete propagation sweeps and two complete paired-basis
  geometric sweeps through `conduct`.
- Use automatic workstation lanes and reusable hidden sessions; do not pass a
  worker count.
- Retain all advice, bindings, material samples, cells, state/orientation
  tables, apertures, Fields, focal regions, focus measures, manifests, and
  exact source references required to reproduce each Result.
- Reopen authority and replay the four briefs without repeating admitted
  advice, solver work, or Field propagation.
- Record the full delivery verification separately from the at-most-fifteen-
  solve native smoke tracer.

## TDD seam

Begin with the fake-backed public four-brief matrix. Run live delivery only
after focused, architecture, type, and CSU checks are green.

## Acceptance

- Johansen and Pi each return 8-, 12-, and 16-state Results.
- Khorasaninejad and Yang each return one continuous-rotation geometric
  Result.
- The matrix therefore returns exactly eight independent Results.
- Every Result uses native Lumerical cell evidence and the admitted Torch
  realization.
- Replay produces byte-identical conclusions and performs no repeated advice,
  solver work, or propagation.
- Focused tests, full tests, architecture checks, Pyright, and CSU pass.
- Rust has no diff.

## Comments

### 2026-07-28 — real conclusions or honest waiting studies

The delivery keeps every canonical brief unchanged. If native evidence or the
fixed focus window cannot close a proof, the accepted outcome is a replayable
waiting Study with exact diagnostics; the run must not tune the brief, widen
the scientific contract, or fabricate a Result merely to reach the nominal
eight-Result matrix. Complete Results remain the target, not a reporting
precondition.

### 2026-07-28 — implementation handoff

The public fake-backed matrix retains all four canonical briefs unchanged and
returns typed, byte-replayable waiting Studies when solver evidence is absent.
Authority reopen restores admitted advice and available science without
repeating the recorded adviser, solver, manifest, or Field seams.

The complete native matrix is a separate, doubly gated live harness. It was
collected but not executed during implementation: running it requires both the
`lumerical_live` marker and `METACRAFT_RUN_LUMERICAL_DELIVERY=1`. A native
failure remains an exact waiting Study for later inspection; it is never
converted into a fabricated Result.

Focused replay and matrix checks passed, architecture checks passed, Pyright
reported no errors, touched-file CSU reported no hard violations, and Rust has
no diff. The default full suite exceeded the bounded sixty-second agent gate
without emitting a failure, so no full-suite pass is claimed here.
