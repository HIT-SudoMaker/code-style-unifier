# 08 — 收回旧 route 编排

**What to build:** 在新 seam 已覆盖当前行为后，删除旧 phase-specific compiler branches、route-owned end-to-end conclusion seams、manual example orchestration 与重复测试，同时保留并归位已经验证的扫参、匹配、aperture、field 和 focus 物理实现。

**Blocked by:** 03 — 让未来目标通过同一种语言; 04 — 让传播相位闭合为 result; 05 — 让 PB 相位闭合为 result; 06 — 让 runner 只负责采集证据; 07 — 让一个 brief 得到诚实答案.

**Status:** wontfix

**Superseded by:** [Metalens Sonnet convergence](../../metalens-sonnet-convergence/spec.md).

- [x] 公开 science surface 只保留 brief、study、result 与必要操作，不暴露第二套 route lifecycle。
- [x] 不再通过 phase-specific compiler branch、package workflow 或 route executor 决定研究过程。
- [x] 已验证的 solver observations 与纯物理实现继续被新 methods 使用，不因组织变化而失效。
- [x] standard brief constructors、manual lifecycle examples 与等价的 implementation-detail tests 被删除或迁移。
- [x] source 与 tests 中不保留 M1–M7、sequence、workflow manager、versioned module name 或其他废弃心智模型。
- [x] contract 过程保持默认 suite 通过，不修改 Rust。

**Verification:** 默认段 `193 passed, 7 skipped, 2 deselected`；传播与 PB 的
`30 µm` 完整 integration 段分别通过；Standards 与 Spec 双审均为 PASS。
