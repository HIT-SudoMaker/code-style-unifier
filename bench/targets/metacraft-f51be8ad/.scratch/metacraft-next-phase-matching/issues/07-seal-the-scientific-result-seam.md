# 07 — Seal the scientific result seam

**What to build:** A maintainer can rely on one hard scientific result seam for
current quantized designs and future pointwise layouts, with obsolete
continuous-matching and nominal-plane paths unable to bypass the new evidence
and fabrication contracts.

**Blocked by:** 03 — Return propagation design results; 06 — Return geometric
design results.

**Status:** wontfix

**Superseded by:** [Metalens Sonnet convergence](../../metalens-sonnet-convergence/spec.md).

- [x] The public route-result interface accepts an exact study, advice record,
  admitted evidence closure, and explicit quantization, and returns one
  immutable design result.
- [x] Legacy per-site full-library matching and single nominal-focal-plane
  result paths are removed after both current routes use the new interface.
- [x] A synthetic pointwise fixture can populate the same cell, state, and
  aperture contract without a phase-level field, while large-na physics remains
  unimplemented.
- [x] Quantized matching has no per-site full-library scoring path, and
  performance verification guards the indexed vectorized assignment.
- [x] Mixed-height libraries, route evidence substitution, stale advice,
  incomplete focus bracketing, duplicate state identity, and malformed
  fabrication geometry fail at the highest public seam.
- [x] Native and deterministic execution sources remain explicit throughout the
  evidence closure, and a non-native fixture cannot be serialized as a native
  scientific conclusion.
- [x] Existing grating construction, monitor offsets, mesh accuracy, qualified
  workstation lanes, capacity freshness, and partial observation reuse remain
  unchanged and covered.
- [x] Domain documentation and historical planning records no longer claim that
  continuous per-site matching or one nominal focal plane completes the current
  low-na capability.
- [x] The complete suite verifies deterministic replay for 8-, 12-, and
  16-level propagation and geometric results.
- [x] Rust source, build inputs, native extension surface, and authority
  protocol are byte-for-byte unchanged by the feature.

## Comments

2026-07-24: Sealed after two-axis review. Both routes expose explicit conclusion
names, use one fixed angular-spectrum convention, require exact aperture,
field, and complete-focus evidence, and reject metadata or evidence
substitution. The complete suite passes with 147 tests and five explicitly
gated live Lumerical tests skipped; Rust is unchanged.
