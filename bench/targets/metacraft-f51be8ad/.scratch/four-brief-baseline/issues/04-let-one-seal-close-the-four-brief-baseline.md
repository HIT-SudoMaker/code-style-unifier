# 04 — Let one seal close the four-brief baseline

**Parent spec:** [Four-brief grounded baseline](../spec.md)

**Decision source:** [Let cleanup follow the four-brief baseline](../../four-brief-grounding/decisions/09-let-cleanup-follow-the-four-brief-baseline.md)

**Status:** resolved (2026-08-12)

**Blocked by:** Ticket 03

## What to change

Perform one integration-only seal after the four retained brief-stage capsules
exist. Reinspect their canonical briefs, material bindings, period/height
answers and choices, order regimes, cautions, Authority references, and exact
`WaitingStudies` endpoints. Record identities and digests in
`.scratch/four-brief-baseline/closure.md`; do not copy scientific authority
into the closure or treat tracker prose as evidence.

Search the current source, examples, tests, active acceptance support, and
navigation for Yun. Remove only live Yun entry points missed by Ticket 02.
Historical ADRs, Research Records, resolved/superseded tickets, transcripts,
and closures retain their original names and contents. Reverify the exact old
run path `runs/brief-stage-yun-20260808-130913-ddc0e3c6` and its reference
status. It may be removed only if the new McClung capsule is retained, no
versioned document points to it, and deletion cannot affect another evidence
chain. Record whether removal was recoverable. Do not generalize that decision
to all `runs/`.

Classify remaining repository-local residue as retained evidence, reproducible
cache/build output, requested reference material, or unknown. Remove no
unknown item and perform no broad recursive purge. The already deleted sibling
`report` and `sonnet-ticket09-20260804-01` directories need only be recorded as
the completed, recoverable cleanup authorized by the grounding decision.

Update `DESIGN.md`, `SCIENCE.md`, `DEVELOPMENT.md`, `.scratch/INDEX.md`, and the
parent ticket statuses only where their owned current truth changed. The
closure must state exact tests, counts, exclusions, capsule outcomes, retained
limitations, cleanup actions, and any dirty-worktree files deliberately left
untouched.

## Acceptance

- The active catalogue and acceptance frame name exactly four cases with no
  live Yun route.
- All four capsules satisfy Ticket 03 independently and are reproducibly
  inspectable without a solver or paper reference.
- Period legality, order classification, and complete-field applicability tell
  one story in ADR, glossary, science docs, code, and tests.
- No production dependency points to examples, acceptance support, `.scratch`,
  or Research Records; runtime import DAG remains acyclic.
- No draft brief, provider client, benchmark-specific science branch,
  compatibility alias, duplicated registry, or second lifecycle exists.
- Cleanup names every removed path and its recoverability and preserves all
  unrelated user changes.

## Verification

Run the complete non-live pytest suite with the repository interpreter,
Pyright, the focused architecture suite, CSU when its repository binary is
available, the local Markdown-link ratchet, and `git diff --check`. Report
skips and exclusions exactly. No integration, Lumerical live/canary/delivery,
Native, paid, field, or performance run may be implied by these gates.

## Stop condition

Close all four tickets only after the seal is green and the closure is
self-consistent. A failed gate reopens its owning ticket; this ticket performs
no semantic repair. Further aesthetic refactoring, sweeps, field calculation,
achromatic work, or broad workspace cleanup requires a new bounded decision.

## Resolution

The four direct-caller capsules are sealed at `WaitingStudies`; their exact
case outcomes, capsule digests, retained limitations, cleanup classification,
and repository gates are recorded in [the closure](../closure.md). The earlier
nested-process campaign remains retained as an honest failed usability
experiment and is not presented as scientific acceptance.
