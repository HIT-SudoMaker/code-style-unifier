# Closure - Harness-native consultation

**Status:** sealed (2026-08-09)

## Conclusion

The embedded provider road is gone. One content-addressed consultation
contract, one resumable conduct life, one installed command Adapter, and one
canonical design skill now form the implemented path. The deterministic seal
is green after its owners repaired stale test fixtures and CSU contract
findings without weakening production science.

The external usability result is not green. Four fresh Codex sessions could
not execute the capsule-local installed command under their workspace policy;
four fresh Claude Code sessions stopped before tool use because authentication
had expired. All eight remain at period consultation with no advice, answer,
period, or height. No session was rerun. This closure therefore claims an
implemented and deterministic architecture, not successful harness use,
Native evidence, paper reproduction, or scientific performance.

## Seal attempts

The focused command for both attempts was:

```powershell
& 'C:\Users\Administrator\miniforge3\envs\research_env\python.exe' -m pytest `
  -q --tb=short -p no:cacheprovider `
  tests\acceptance tests\command `
  tests\science\test_metalens_brief_document.py `
  tests\science\test_period_consultation.py `
  tests\science\test_height_consultation.py `
  tests\science\test_application_root.py tests\science\test_conduct.py `
  tests\science\test_period_height_domain.py `
  tests\science\test_height_choice.py tests\science\test_study_codec.py `
  tests\authority\test_strict_decoding.py `
  tests\examples\test_metalens_benchmark_cases.py `
  tests\examples\test_yun_benchmark_contract.py `
  tests\examples\test_yang_benchmark_contract.py `
  tests\examples\test_arbabi_benchmark_contract.py `
  tests\examples\test_khorasaninejad_benchmark_contract.py `
  tests\architecture\test_runtime_import_dag.py `
  tests\architecture\test_scientific_boundary.py `
  tests\architecture\test_sonnet_architecture.py `
  tests\architecture\test_sonnet_ratchets.py `
  tests\architecture\test_domain_naming.py
```

The full deterministic command for both attempts was:

```powershell
& 'C:\Users\Administrator\miniforge3\envs\research_env\python.exe' -m pytest `
  -q --tb=short -p no:cacheprovider `
  -m 'not integration and not lumerical_live and not lumerical_delivery and not lumerical_canary'
```

- Attempt 1 first exposed one stale 400 nm cross-family period fixture:
  507 passed and 1 failed. Ticket 02 made that non-normative preference choose
  through each admitted legal PeriodDomain; the focused gate then reached 508
  passed.
- Attempt 1 full suite recorded 1339 passed, 5 failed, and 5 deselected in
  350.80 seconds. Three diagnostic paths still requested an illegal fixed
  period; two propagation paths recommended a height explicitly ruled out by
  their exact PhaseEnvelope.
- Ticket 02 unified those period fixtures behind one largest-legal-candidate
  selector. Ticket 03 made height fixtures select only legal, non-ruled-out
  candidates and carry that choice through the Result DAG. No production
  ceiling, envelope rule, benchmark truth, or validation boundary changed.
- Attempt 2 focused gate passed 508 tests in 76.41 seconds.
- Attempt 2 full suite passed 1344 tests with 5 deselected in 411.69 seconds.

The five deselected groups are exactly `integration`, `lumerical_live`,
`lumerical_delivery`, and `lumerical_canary` as expressed by the marker
predicate above. No skip or unavailable Native check is represented as a pass.

## Final deterministic gates

- Pyright: 0 errors, 0 warnings, 0 information findings.
- Architecture: 114 passed.
- Retained harness acceptance: 10 passed after the final Standards correction.
- CSU: the documented full `src\metacraft` command completed with 0 blocking
  findings; 4614 non-blocking `under_review` findings remain review evidence,
  not silently discarded passes.
- Markdown links: 1 passed with 19 non-link ratchets deselected. The ratchet now
  reads tracked and untracked nonignored Markdown rather than only the Git
  index. Ignored local reference material remains outside the repository
  contract.
- `git diff --check`: passed; Git emitted only the existing `.gitignore`
  LF-to-CRLF working-copy warning.

The CSU command was:

```powershell
.\csu\bin\csu.exe check src\metacraft --format json `
  --output .csu\report.json --no-history
```

Final Standards review then tightened the direct PeriodAdvice boundary to the
same closed identity and EvidenceRequired rules as HeightAdvice. That bounded
correction changed no candidate, choice, physics, or conduct transition. Its
post-seal verification passed 238 period/height/strict-decoding tests, Pyright
with zero findings, CSU with zero blocking findings, and `git diff --check`.
The same review made transcript confinement reject missing or unknown event
types, malformed required fields, case-changed tool names, and unapproved file
changes. The retained eight-session evidence still passes unchanged; the
focused acceptance verification passed 10 tests and Pyright with zero findings.
The final unified post-review gate passed 523 focused tests; project Pyright,
the Markdown-link ratchet, and CSU again passed with zero blocking findings.

## Harness evidence inventory

The retained evidence root is `acceptance/07` beneath this feature directory.
It contains 8 transcripts, 8 audits, 8 outcomes, 8 stderr records, 5 post-hoc
reports, and 0 answer files. The empty answer count is the expected record of
the failed sessions, not missing evidence.

The manifest chain retains the original seal, audit correction, audit-corrected
manifest, retained-evidence correction, and current sanitized manifest. Both
corrections record zero session reruns. The current 8 audits contain no
network/search/MCP event, outside-capsule access, invalid write, invalid JSONL,
or unreviewed command. Post-hoc reports keep advice and published facts
separate and invoke no benchmark comparison contract, signed difference, or
acceptance threshold.

Codex `0.146.0` and Claude Code `2.1.220` are the tested executables. Reasonix
and Pi remain unverified future candidates and have no Adapter, prompt copy, or
support statement.

## Source and skill inventory

- The installed Python root exports exactly `Authority`, `compile_study`, and
  `conduct`.
- The runtime import graph is acyclic; production imports neither examples nor
  the command/skill layer.
- Production source contains zero occurrences of `METACRAFT_LLM_*`,
  `OpenAICompatibleAdviser`, `LlmConfig`, `advice_live`, or the four retired
  advice schema identifiers.
- The four provider package files, broad design/wording advice files, four old
  provider tests, and `.env.api.example` are deleted.
- Exactly one canonical `skills/metacraft-design/SKILL.md` exists. The Codex and
  Claude project routers are byte-identical redirectors to that source.

The cutover inventory consists of the accepted ADR and research records;
`CONTEXT.md`, `DESIGN.md`, `SCIENCE.md`, and `DEVELOPMENT.md`; the exact brief,
consultation, period/height advice, Study restoration, conduct, command, and
material-binding seams; the command entry point and shared skill; focused
Authority/science/command/architecture/benchmark tests; the eight-session
evidence chain; and this specification, map, eight tickets, index entry, and
closure record. The associated reviewed benchmark briefs and material aliases
are prerequisite records, not benchmark truth changes.

The versionable path inventory is exact at seal time:

- Configuration and canonical records: `.env.api.example` (deleted),
  `.gitignore`, `pyproject.toml`, `CONTEXT.md`, `DESIGN.md`, `SCIENCE.md`,
  `DEVELOPMENT.md`, `docs/adr/README.md`, ADR 0021, both 2026-08-08 Research
  Records, `.scratch/INDEX.md`, and this feature's `spec.md`, `map.md`, eight
  issue files, `closure.md`, and `acceptance/07/**`.
- Native skill surfaces: `skills/metacraft-design/SKILL.md`,
  `.agents/skills/metacraft-design/SKILL.md`, and
  `.claude/skills/metacraft-design/SKILL.md`.
- Added production modules: `src/metacraft/command.py` and
  `src/metacraft/science/consultation.py`.
- Changed production modules: `src/metacraft/authority/protocol.py`, both
  `src/metacraft/materials/{__init__,solver}.py`,
  `src/metacraft/science/{__init__,_application_root,conduct,study}.py`, and
  `src/metacraft/science/metalens/{brief,compiler,conduct,consultation,height,
  height_advice,period,period_advice}.py`.
- Deleted production modules: all four tracked files under
  `src/metacraft/advice/`, plus
  `src/metacraft/science/{design_advice,wording}.py`.
- Added focused tests/support: `tests/acceptance/**`, `tests/command/**`,
  `tests/harness_acceptance.py`, `tests/harness_acceptance_runner.py`,
  `tests/materials/test_material_confirmation.py`, and
  `tests/science/{test_height_consultation,test_metalens_brief_document,
  test_period_consultation}.py`.
- Changed or deleted regression support: the four deleted `tests/advice/`
  files; `tests/architecture/{test_domain_naming,test_runtime_import_dag,
  test_scientific_boundary,test_sonnet_architecture,test_sonnet_ratchets}.py`;
  `tests/authority/test_strict_decoding.py`; `tests/{domain,lumerical,
  propagation,result}_fixtures.py`; `tests/examples/metalens_diagnostic_support.py`;
  `tests/examples/{test_arbabi_benchmark_contract,
  test_khorasaninejad_benchmark_contract,test_metalens_benchmark_cases,
  test_recorded_metalens_evidence,test_yun_benchmark_contract}.py`;
  `tests/materials/test_solver_material_library.py`;
  `tests/science/{test_application_root,test_conduct,
  test_geometric_cell_choice,test_height_choice,test_period_height_domain,
  test_pointwise_result,test_propagation_delivery,test_standard_studies,
  test_study_codec}.py`; `tests/solvers/test_lumerical_qualification.py`;
  and `tests/workstation/test_workstation.py`.
- Reviewed prerequisite records: the three existing four-brief-grounding
  records plus decision 05, `.scratch/solver-material-sonnet/pending-materials.md`,
  the four `examples/metalens_benchmark/*.py` cases, and
  `materials/lumerical.toml`.

## Exclusions and stop

No Native solve, Lumerical session, parameter sweep, network request, harness
rerun, MCP/plugin framework, user-global configuration edit, `.env` cleanup,
Reasonix/Pi execution, or unrelated workspace cleanup occurred in the seal.
The feature stops here. A future change must name a failed owner contract or a
new bounded acceptance proposal; aesthetic possibility alone does not reopen
the architecture.
