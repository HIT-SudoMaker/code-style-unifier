# 08 - Let one deterministic seal close the harness cutover

**What to build:** Verify the provider-free consultation architecture,
resumable conduct life, command Adapter, shared skill, and retained harness
evidence as one bounded Sonnet result. The seal records facts and repairs no
semantic implementation.

**Blocked by:** 07 - Let two harnesses judge four blind cases.

**Status:** resolved (2026-08-09; deterministic seal green)

- [x] ADR 0021, CONTEXT.md, canonical design/science/development documents,
      specification, code, tests, skill, command help, and transcripts use one
      consultation language and exact supersession boundary.
- [x] Remove ADR 0021's `Implementation status: pending` only after every owning
      ticket has updated code, tests, and canonical documentation in the same
      slice.
- [x] Brief validation, period request, height request, answer validation,
      advice admission, conduct resumption, command translation, and harness
      instructions each have one owner and one-way dependencies.
- [x] No embedded model/web request, provider configuration, provider-shaped
      advice, active consultation callback, second lifecycle, pending-request
      state, compatibility reader, copied prompt, or harness-specific science
      remains.
- [x] The installed Python root still exports exactly Authority, compile_study,
      and conduct; production imports neither examples nor the command/skill
      layer; the runtime import graph is acyclic.
- [x] Focused request, answer, advice, conduct, command, compiler, choice,
      environment, canonical, Authority, benchmark, and architecture tests pass.
- [x] The full deterministic non-live suite passes with no newly hidden skip;
      Pyright, Markdown links, domain naming, source analysis, and `git diff
      --check` pass.
- [x] Eight clean-session transcripts exist and accurately distinguish harness
      success, failure, disagreement, and EvidenceRequired. Reasonix and Pi
      remain future candidates in every support statement.
- [x] Every transcript proves network/search denial and capsule path confinement;
      every post-hoc report remains outside the benchmark Result comparison
      contract and contains no signed delta or threshold.
- [x] The final inventory contains only the approved decision/canonical docs,
      consultation and conduct migration, command/skill artifacts, focused
      tests, benchmark harness evidence, and closure records.
- [x] No Native solve, solver/template/physics change, benchmark truth change,
      MCP/plugin framework, user-global configuration edit, unrelated cleanup,
      or credential content is present.
- [x] Exact counts, exclusions, command outcomes, transcript inventory, and
      source-absence evidence are appended to one closure record.

## Stop condition

Close when every deterministic contract and the eight honest session records
agree. A failing contract reopens its owning ticket; the seal does not patch it.
Further aesthetic possibility is not a reason to continue.

## Comments

The exact attempt history, owner reopenings, green final gates, retained
harness failure, inventory, and exclusions are recorded in
[closure.md](../closure.md). The seal repaired no scientific semantics: owner
tickets made stale fixtures consume their exact legal domains and made CSU
contract/document layout conform without changing runtime behavior.
Final Standards review then reopened only the PeriodAdvice contract and
Ticket 07 confinement-audit owners; their bounded corrections and focused
verification are recorded in the closure without rerunning a harness session.
