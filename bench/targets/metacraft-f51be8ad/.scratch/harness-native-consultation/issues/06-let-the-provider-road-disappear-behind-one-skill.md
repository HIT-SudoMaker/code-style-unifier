# 06 - Let the provider road disappear behind one skill

**What to build:** Complete the one-time contraction: remove the embedded
OpenAI-compatible transport and its provider-shaped science, then publish one
concise MetaCraft design skill that drives the installed command in Codex and
Claude Code.

**Blocked by:** 05 - Let one command carry the consultation cadence.

**Status:** resolved (2026-08-09; final contract correction)

- [x] Delete OpenAICompatibleAdviser, LlmConfig, LLM environment loading,
      urllib transport, provider parsing, package exports, `.env.api.example`,
      the `advice_live` marker, and live-provider tests.
- [x] Delete broad DesignAdvice, Suggestion, provider-shaped WordingReview and
      AdviceStatus after all legitimate tests use exact brief validation or
      grounded consultation values.
- [x] Replace provider-oriented fakes and fixtures with canonical requests,
      answers, admitted advice, or exact recorded evidence through production
      Interfaces.
- [x] Preserve the general invariant that unrelated secrets cannot cross into
      solver process environments; rename its fixture to a generic prohibited
      secret rather than deleting the safety check.
- [x] Add architecture/source ratchets forbidding the retired names,
      `METACRAFT_LLM_*`, language-model HTTP code, provider fields, old schemas,
      and production imports of command or skill Modules.
- [x] Make each request's identity-covered answer contract sufficient for a
      clean harness to write the exact answer document without source
      inspection or a schema copy in the skill.
- [x] Add the sole behavior source at `skills/metacraft-design/SKILL.md` with the
      cadence clarify -> conduct -> reason/research -> conduct with answer ->
      continue/stop.
- [x] Add byte-identical native routers at
      `.agents/skills/metacraft-design/SKILL.md` and
      `.claude/skills/metacraft-design/SKILL.md`. Each contains only discovery
      metadata and an instruction to read
      `../../../skills/metacraft-design/SKILL.md`; architecture tests assert
      equality, target existence, and absence of scientific policy.
- [x] Keep the skill concise and action-oriented. It contains no numerical
      rules, material alias table, prompt copy, response schema copy, paper
      values, solver policy, or Codex/Claude-specific tool vocabulary.
- [x] Document separate installation steps only where the two harnesses differ;
      do not add plugin manifests, session hooks, MCP, or global-user edits.
- [x] Update `CONTEXT.md`, `DESIGN.md`, `SCIENCE.md`, and `DEVELOPMENT.md` in the
      same cutover so no canonical document still describes the provider road.

## Verification boundary

The feature must have exactly one supported consultation road after this
ticket. Run deterministic science, command, architecture, import, environment,
and source-absence tests. Inspect local `.env` only to report obsolete keys;
never print, commit, or silently delete a user's credential. No harness session
or Native solver runs here.

## Comments

Removed the embedded model transport, its configuration and live marker, the
broad provider-shaped advice values, and their tests. Existing scientific
tests now form canonical period and height requests, answers, and advice through
production Interfaces. The solver-process isolation checks retain one neutral
`METACRAFT_PROHIBITED_SECRET` fixture.

Published one concise canonical design skill and two byte-identical project
routers. `.gitignore` exposes only the exact Claude router under otherwise local
Claude state; no plugin, MCP, hook, or global configuration was added. Local
`.env` inspection found obsolete `METACRAFT_LLM_*` key names; no values were
read or printed and the file was not modified.

Verification: 231 focused science, command, and material tests pass; 112
architecture tests pass; Pyright reports zero errors and warnings. The source
ratchets report no retired production path, identifier, configuration literal,
language-model transport import, provider field, or production command import.
No harness, Native, or broad numerical gate was run.

The final correction deepened the request-owned answer contract with the exact
answer schema, conclusion members, indexed external-claim shape, canonical
claim-identity rule, and locator rule. It also closed the last provider/model
transport ratchet gaps across consultation owners, corrected ADR 0003's narrow
credential-clause supersession, and made the Claude ignore exception exact.
Focused request, command, documentation, and architecture verification passes
63 tests. Ticket 07 acceptance code and sessions were not touched.
